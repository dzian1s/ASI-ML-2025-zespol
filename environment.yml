name: asi-ml
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - jupyterlab
  - ipykernel
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - pip
  - pip:
      - pre-commit
      - ruff
      - black
      - pytest
      - wandb
      - kedro
      - kedro-datasets[pandas,pickle,tracking]==2.1.0
      - pyarrow
      - pytest


  - pyyaml
